# ProductiveBees_zh-cn — 资源蜜蜂简体中文汉化
# Copyright (C) 2026 星野夢華 (Hoshino Yumeka)
# SPDX-License-Identifier: GPL-3.0-or-later
name: Build

# 出**全部平台**的 jar。一次 build 全出，各平台只差字节码目标与加载器元数据，
# 数据源只有 src/ 那一份——不会为哪个整合包单独编一个。
#
# 比 CI 慢：要按 targets.json 逐个下资源蜜蜂的 jar（导览书是拿映射现套到它自带
# 那份 JSON 上生成的，非有不可），再逐个平台编译。
on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（x.y.z / x.y.z-beta.N / x.y.z-rc.N）'
        type: string
        required: false
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'mod/**'
      - 'scripts/**'
      - 'versions/**'
      - 'deps.lock.json'
      - 'manifest.json'

jobs:
  build:
    name: 全平台构建
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 矩阵里的平台横跨 Java 8 / 17 / 21，三个都得在
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: |
            8
            17
            21

      - name: 定版本号
        id: v
        run: |
          set -euo pipefail
          v='${{ inputs.version }}'
          # 非发布构建用 0.0.0-rc.N：既满足 mods.toml 的版本形态，
          # 又一眼看得出不是正式版（N 取运行号，保证递增不重复）
          [ -n "$v" ] || v="0.0.0-rc.${GITHUB_RUN_NUMBER}"
          echo "version=$v" >> "$GITHUB_OUTPUT"

      # 资源蜜蜂的 jar 与 Gradle 都按 sha256 核过才用；缓存命中也照样核
      # （build.py 自己核，缓存里的东西同样可能是坏的）。
      - uses: actions/cache@v4
        with:
          path: |
            build/pbjars
            build/gradle
            ~/.gradle/caches
          key: deps-${{ hashFiles('versions/targets.json', 'deps.lock.json') }}

      - name: 出全部平台的 jar
        run: python3 scripts/build.py '${{ steps.v.outputs.version }}'

      - name: 产物清单
        run: ls -lh dist/

      - uses: actions/upload-artifact@v4
        with:
          name: productivebees-zh_cn-${{ steps.v.outputs.version }}
          path: dist/*.jar
          if-no-files-found: error
