# Changelog

## 1.0.1

**补 16 只蜂的中文名**，之前它们的蜂名和基因样本都显示英文：

| 来自 | 蜂 |
|---|---|
| 末影接口：进化 | 粗钢、充能银、生动合金、晶化合金、旋律合金、恒星合金、晶化粉红史莱姆、铁合金 |
| Civilization Isolaria | 超越合金、末地合金、下界合金、源之宝石、铀 |
| Mega Showdown 矿物 | 化石、极巨矿、Z 纯晶 |

名字尽量跟着那个模组自带的中文走——末影接口：进化 那 8 条就是照它自己的
`zh_cn.json` 定的，这样蜂和材料在游戏里是同一个叫法。

**修四组译名错误**（对抗核验查出来的，逐条核过证据才改）：

| 键 | 原来 | 改成 | 依据 |
|---|---|---|---|
| Regions Unexplored 的三种木料（蜂箱与扩容盒共 6 条）| 黑木 / 枯叶木 / 槿紫木 | 黑檀木 / 亡灵木 / 魅紫木 | 该模组自带 zh_cn 里就是这三个名字 |
| `item.productivebees.honeycomb_bauxite` | 铝土蜜脾 | 铝土矿蜜脾 | 同族的蜂与蜜脾块本来就叫「铝土矿」 |
| `entity.productivebees.reed_bee`（含刷怪蛋与导览书 5 处）| 芦苇蜜蜂 | 甘蔗蜜蜂 | 它的巢 Reed Nest 已译「甘蔗蜂巢」，风味文本也写「甘蔗丛中」 |
| `entity.productivebees.imperial_bee` | 高级精华蜜蜂 | 帝王蜜蜂 | 来自 Modular Bees，与神秘农业的 Imperium 撞名了；「女王蜜蜂」已被 `royal_bee` 占用 |

**导览书里的术语和物品名对齐**（14 处）。书里有几样东西的叫法跟物品栏里对不上，
一律照物品名改：

| 书里原来 | 改成 |
|---|---|
| 育种室 | 繁殖箱 |
| 捕手 | 捕捉器 |
| 喂食台阶 | 饲养板 |
| 龙蛋蜂巢 | 龙蛋蜂箱 |
| 蜂蜜大餐 | 蜜蜂小食 |

升级卡说明里的「捕手」「喂食台阶」也一并改了。全部版本受影响。

## 1.0.0

**加了 mod 图标。** 模组列表和 README 都用同一张。原图是不透明 RGB、底色纯黑，
直接用就是个黑方块，所以先抠底：从四边泛洪、只抠与边框连通的纯黑。阈值卡在
纯黑 (0,0,0) 与辉光最暗处 (0,56,142) 之间并贴着黑那一侧——够到辉光的话，泛洪会
顺着辉光走进主体，再从蜜蜂那圈同样是纯黑的轮廓穿出去，把轮廓和眼睛一起吃掉
（第一版就是这么废的）。抠图与缩放由 `scripts/logo.py` 现做，只用标准库。

**修 1.15.2 上蜂名还是英文：不再跟数据包抢加载顺序。**

beta.4 的办法是随包发一份同路径的蜂数据、把 `name` 填成中文，指望它盖住模组
自带那份。测试者 1.15.2 的日志证明这条路靠不住——数据包顺序是这样的：

    Reloading ResourceManager: productivebees-zh_cn-….jar, jei-….jar,
                               Default, forge-….jar, productivebees-1.15.2-….jar

后面的盖前面的：我们排第一，模组本体排最后，我们那份被整个盖掉。同一个包在
1.16.4 上顺序恰好相反就生效了——这种看运气的东西不能留。

改成**等它加载完再动**：`BeeReloadListener.INSTANCE.getData()` 是模组自己公开的
方法，返回「蜂 id → NBT」那张表。数据包谁赢都无所谓，最后落进这张表的就是它，
我们把 `name` 改掉，物品栏、手持、JEI 三条路读到的就都是中文。

- 只改**还是英文**的那些；已经是中文的（数据包赢了、或模组自带中文）一律不碰
- 找不到类、找不到方法、表是空的，全都直接返回——补不上顶多显示英文
- 随包那份蜂数据保留：它赢的时候结果一样，输了也有这一层兜底

**整合包自己加的蜂，名字改成运行期现学。**

整合包用数据包给资源蜜蜂加蜂，`entity.productivebees.tiberium_bee` 这种键不在
模组本体里，我们的语言文件自然也没有。原先的办法是把整合包一个个扫过来、材料名
一条条手写进词表——但那只覆盖扫过的包。拿留一包检验量过：**换一个没扫过的整合包，
只有 28.4% 能对上**，七成照样是英文。

症结在于「tiberium 的中文是什么」本来就不该由我们知道，那是别的模组的专有名词。
但玩家的游戏里知道——他装了那个模组的官方中文，或者装了社区汉化包。所以改成
从当前语言表里现查：找这个材料的基础形态条目（`tiberium_ingot` = 泰伯利亚锭），
去掉量词投票，七成以上的票投给同一个词才认，再按形状拼成「泰伯利亚蜜蜂」。

拿 All the Mods 10 那 481 个模组的语言文件当「玩家的游戏」实测，合成 383 条
没见过的整合包蜂键：

| | |
|---|---|
| 学对 | 259 条（67.6%） |
| **学错** | **0 条** |
| 学不出（保持英文） | 124 条 |

零学错是刻意换来的。**把游戏搞崩是 P0，把名字翻错也是 P0；少翻一条只是个
issue。** 所以拿不准一律不翻：两个模组对同一个 id 有两种译法（`ironwood` 在暮色
森林叫铁木、在别处叫铁色木）票会分裂，放弃；只有一种形态、样本不足，放弃；
样本是工具盔甲而非基础材料（`fiery_sword` 炽焰剑、`fiery_blood` 炽热的血液），
不算票。

这样还顺带对了一件事：**跟着玩家自己那份汉化的叫法走**，不会出现蜂叫这个、
矿叫那个的两张皮。玩家压根没装那个模组的中文时也查不到，就保持英文——那正好和
他游戏里其余部分一致。

- 全表**只扫两遍**，不是每个蜂键重扫一遍。语言表在大整合包里有十几万条、
  资源蜜蜂的键上千，逐个重扫是上亿次字符串比较，一次就能把客户端卡住好几秒——
  玩家眼里那和崩溃没区别。实测 15 万条的语言表，一次 37–74 ms，
  而且只在资源重载后跑一次（对象身份没变就立即返回）
- 取语言表**读静态字段，不调方法**。拿官方混淆表核过：1.16.5 与 1.21.1 的
  `Language` 上都有两个静态无参、返回自身类型的方法——`getInstance()` 和
  `loadDefault()`；后者会当场读一份全新的默认语言表回来，按形状挑就是碰运气
- 动那张 Map 之前先确认它**真的是语言表**（条数够、且含 `gui.done` 这类原版键），
  不是「只要是个 Map 就往里塞」
- 注册监听器那一句也用 try 兜住：注册不上顶多是这几只蜂不翻，不能因此让 mod 装不上
- 任何一步够不着都整段放弃，不抛异常：补不上顶多是那几只蜂显示英文
- CI 加一道自验，正反都验：该学出的要学对，**拿不准的必须学不出**

## 1.0.0-beta.4

测试：[十一月の风筝](https://space.bilibili.com/2041176282)

**修「绿宝石蜜蜂蜜蜂」。** `entity.productivebees.bee_configurable` 的英文原文是
**裸的一个 `%s`**——整条内容都来自运行期传进来的那个参数（数据包定义的蜂，名字
存在实体 NBT 里，形如 `Emerald Bee`）。我们把它译成了 `%s蜜蜂`，于是那两个字
直接附在该参数之后：物品栏里显示「绿宝石蜜蜂蜜蜂」，JEI 里显示「Draconic Bee蜜蜂」，
手持时显示「Lapis Bee蜜蜂」。改回 `%s`。

用到这个键的是 1.15.2 / 1.16.1 / 1.16.3 / 1.16.4 四个版本；再往后模组代码里已经
没有任何一处引用它，所以新版本不受影响。

- 加一道出包闸：英文原文除了占位符和标点一个字母都没有的条目，译文也不许加字。
  拿出问题那一版的字节反过来试过，闸会红。全表体检只有这一条中招

**修数据包被标红、建世界弹「实验性选项」。** 1.19 那几个版本里，资源包格式号与
数据包格式号**不是同一个数**（9 与 10），而我们只写了一个 `pack_format`。游戏拿它
去校验数据包那一侧，对不上就把这个包判成不兼容。Forge 为此有
`forge:resource_pack_format` / `forge:data_pack_format` 两个字段，上游 jar 一直
这么写，我们没写。

- `pack.mcmeta` 改成照参考 jar 抄，只替换 description。这样格式号该写几个字段、
  各是多少，不再由我们维护第二份表（`_comment` 这类上游自用的注释字段不抄）

**顺带把手持与 JEI 那两处也翻了。** 这两处的文本由游戏和 JEI 各自渲染，都不经过
`ItemTooltipEvent`，钩子改不到——但那个名字的**源头**是数据，不是代码：

    nbt.putString("name", json.has("name") ? json.get("name").getAsString()
                                           : idToName(id) + " Bee");

数据包里给了 `name` 就用给的，没给才按 id 拼一个英文的。所以照抄上游那份蜂的
数据文件、只把 `name` 填成中文，其余字段一个不动，三条渲染路径读到的就都是
中文了，一行代码都不用改，也不必上 Mixin 碰渲染。

- 1.15.2 补了 147/152 只（余下 5 只没有对应译名，不改动上游文件）、
  1.16.1 补了 79/79、1.16.3 与 1.16.4 各 153/153
- 生成的文件逐个核过：除 `name` 外与上游逐键一致
- 只对真正用 `bee_configurable` 的那四版生成；再往后蜂名走注册实体的翻译键，
  本来就是中文，不做改动

## 1.0.0-beta.3

**修一个装上就崩的错：依赖块写成了 `type="required"`。**

1.20.2 之前的加载器（也就是全部 Forge，以及 1.20.1 的 NeoForge）在依赖块里认的是
`mandatory=true`；`type` 是 NeoForge 1.20.2 换用 `neoforge.mods.toml` 时才改的写法。
写错的后果不是「这一个 mod 不加载」，而是加载器在**扫描阶段**抛
`InvalidModFileException`，整个 mods 目录的扫描随即中断——老版本直接闪退，新一些的版本
进得去但一个模组都没加载。beta.2 的 18 个平台全中，只有 1.21 那两个是对的。

- 这一版起，元数据的文件名与依赖写法**照参考 jar 抄**：目标那一版的资源蜜蜂 jar
  就装在同一个 mods 目录里、由同一个加载器读，它加载得了，它用的就是对的写法。
  不再按版本号推——推错了没有任何东西会拦住
- 加一道出包闸：每个依赖块都必须带这一版该带的必需标记，且不许混进另一套写法。
  拿 beta.2 那一版的字节反过来试过，闸会红
- `displayTest` 只写进 `neoforge.mods.toml`。1.17–1.20 的 Forge 根本不读这个键
  （`fmlloader` 与 `fmlcore` 里没有这个字符串），写了也不生效：那几版一直缺
  「服务端没有这个 mod 也没关系」的声明。改成在代码里注册扩展点，1.15 到 1.20
  全部覆盖，进服不再可能被判定为 mod 不一致

**覆盖面从一个平台扩到两个**：CurseForge 2440 个（走反向依赖接口）+ Modrinth
15316 个（那边没有反向依赖接口，只能把整合包全列一遍逐个翻），共翻过 17549 个
整合包与模组。62 个自带蜂名，191 条模组本体没有的 key 现在一条不缺。

- 扫描全部搬上 CI，按分片并行；结果按片缓存，扫过的不再重扫
- 整包不下载：走 HTTP Range 只取压缩包中央目录，再单独取要的那几条语言文件
- Modrinth 的清单按分面切着列——搜索接口一趟翻不过 10000 条，不切就有两千多个
  整合包永远扫不到
- CI 加两道闸：扫描器合成自验，以及「扫出来的整合包 key 一条都不许没译」

## 1.0.0-beta.2

**新增 Minecraft 1.15.2 / 1.16.1 / 1.16.3 / 1.16.4 / 1.16.5（Forge）**，
连同原有版本共 **20 个平台**，全部由同一份译文出包。

- 老版本这条路不走 Gradle：ModDevGradle 建立在 NeoForm 上够不着 1.16，
  而这个 mod 一个 Minecraft 方法都不直接调（按类型反射），也就不需要重映射。
  编译期只要 Forge 官方 jar + Gson 2.8.0，两样都按 sha256 钉死
- 老版本运行期该反射哪个方法名，由**那一版的官方混淆表与 SRG 映射表联结算出**，
  不是猜的；算不出就不出包
- 修 1.17.1 编译失败：`JsonParser.parseReader` 是 Gson 2.8.6 才有的静态方法，
  那一版 Minecraft 自带的 Gson 比它老。改用各版本都在的 `Gson#fromJson`

**整合包自定义的蜂名也翻了。** 整合包会用数据包给资源蜜蜂加自己的蜂，名字由
整合包自己给——这些 key 不在模组本体里，正是各整合包里显示英文的地方。
覆盖了哪些整合包见 README。

- 别的模组的专有名词一律跟约定俗成的译名：暮色森林的钢叶／铁木／骑士金属、
  以太的赞岩／重力石、Psi 的灵能金属、质量效应的零号元素、星际迷航的双锂、振金
- 拼不出「X蜜蜂」的走整条覆盖：Cobbee 是圆石与蜂的双关，译作圆石蜂

其他：

- 导览书补齐：新版本里同样一直有漏翻（升级页的「可用于：…」有两句从来
  没被翻到过）。以前只测「映射命中率」——那测的是我们的条目落没落到上游文件上，
  测不出书里还剩多少英文。改成测产物侧，20 个平台现在一处不剩
- 整合包自定义蜂名的扫描全面返工。CurseForge 反向依赖接口的分页参数是 `page`
  不是 `index`，之前那版把第一页反复取了 N 遍——「扫了 80 个项目」实为 1 个。
  另外整合包的语言文件多半在内嵌的资源包/数据包 zip 里，得再拆一层；
  而附属模组会把资源蜜蜂的 key 写在**自己命名空间**的语言文件里，
  按文件路径找不到，得按 key 的点分段判断
- 扫描器现在带自验：合成用例 + 整包暴力对照（不看路径逐个 JSON 翻，
  和快路径比对，差额直接列出来）；合成用例每次 CI 都跑
- 发版说明的取法补上兜底：精确比对 → 回落到 CHANGELOG 第一段 → 还取不到就失败，
  并且**每次构建都跑**。上一版发出去时说明是一句「本次发布的说明见 CHANGELOG.md」，
  就是因为只有精确比对这一层，落空了还继续发

## 1.0.0-beta.1

首个版本。

- 汉化物品、方块、蜜蜂名、界面、进度：对着 1.21.1 那一版的 1118 个语言条目全部译出
- 汉化内置导览书《蜜蜂大书》全部 81 个页面
- 汉化基因样本 / 蜜蜂小食 tooltip 里的蜂种名，共 467 个蜂种
- 支持 Minecraft 1.17.1 – 1.21.1（Forge / NeoForge），各版本覆盖率见 README
